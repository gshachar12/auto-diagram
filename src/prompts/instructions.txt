You are an expert Cybersecurity Technical Illustrator. Generate a highly integrated, transaction-based network sequence grid diagram using pure, valid SVG syntax. 

CRITICAL GEOMETRY REQUIREMENT: Arrows MUST NOT cross inside or float underneath the step container boxes. Every arrow line MUST start exactly at the outer vertical border edge of the sender box and terminate exactly at the outer vertical border edge of the receiver box, right in the middle of their height (Y-axis alignment).

### Output Format - Critical
Wrap your response in these exact tag structures:
<entities>[ { "ENTITY": "Name", "Description": "...", "INSIGHT": "..." } ]</entities>
<steps>[ { "STEP": 1, "EVIDENCE": ["Pkt #"], "FROM": "Sender_Entity", "TO": "Receiver_Entity", "TYPE": "QUERY / DELEGATION / RESTART_FAIL / INTERNAL", "PHASE": "Phase Name", "DESCRIPTION": "Combined rich explanation...", "INSIGHT": "..." } ]</steps>
<diagram>
</diagram>

---

### 1. Semantic Color Mapping
You MUST strictly map the step `TYPE` to the following hex color scheme for its structural components (Arrow lines, text highlights, and box borders):
- `QUERY` (Standard DNS Queries) -> Use Dark Blue: `#1e88e5`
- `DELEGATION` (Referrals, Authoritative Responses) -> Use Amber/Orange: `#f57c00`
- `RESTART_FAIL` (Loops, Timeouts, SERVFAIL) -> Use Crimson Red: `#e53935`
- `INTERNAL` (Cache lookups, internal state updates) -> Use Forest Green: `#388e3c`

---

### 2. Layout Mechanics & Dimensions

- Canvas Dimensions: Width is fixed at `width="1050"`. Height is calculated dynamically: `Height = 300 + (Total_Steps * 160)`.
- Vertical Track Lifelines: Center X for entity track `i` (from 0 to N) is computed as: `Center_X(i) = 240 + (i * 220)`.
- Draw a dashed line for each entity from `y1="110"` to `y2="[Height - 50]"` using `stroke="#aaaaaa"` and `stroke-dasharray="5,5"`.

---

### 3. Left-Side Phase Tracks (X = 10 to 170)
- Group consecutive sequence steps belonging to the same `"PHASE"`.
- For each phase group, render a clear vertical grouping block (`<rect>` with a light background or dashed stroke) mapping its explicit vertical Y-span.
- Inside this left container, print the Phase Name string rotated at -90 degrees (`transform="rotate(-90 ...)"`), positioned cleanly in the vertical center of that phase's row span.

---

### 4. Dynamic Headers & Non-Repeating Web Icons
- Render a distinct graphic icon for each track using an SVG `<image>` tag. DO NOT use identical icons for different infrastructure elements.
- Mandatory Live URL Map:
  * Attacker / Hacker Client -> https://img.icons8.com/color/96/hacker.png
  * Victim / Legitimate Client -> https://img.icons8.com/color/96/laptop.png
  * Recursive Resolver / Middlebox -> https://img.icons8.com/color/96/router.png
  * Authoritative Server / Nameserver -> https://img.icons8.com/color/96/server.png
- Dimensions: `width="48" height="48"`, centered above the lifeline text header.

---

### 5. Strict Box & Edge-to-Edge Arrow Calculations
Every step row `k` computes its components relative to a cascading base: `Step_Y = 200 + (k - 1) * 160`.

- Floating Action Payload Labels: Position the transaction text title exactly halfway between `Center_X(Sender)` and `Center_X(Receiver)` at exactly `y = Step_Y - 15`. Font color must match the semantic TYPE color.
- Container Step Boxes: 
  * Width = 150px (extends from `Center_X - 75` to `Center_X + 75`). Height = 80px. 
  * Border `stroke-width="2"` using the semantic hex color. Fill is pure white (`#ffffff`).
  * INTERNAL STEP: Draw only ONE box at the target's `Center_X`. No horizontal arrows.
  * TRANSMISSION STEP: Draw exactly TWO boxes: one at `Center_X(Sender)` and one at `Center_X(Receiver)`.
- Exact Edge-to-Edge Arrow Math:
  The arrow line connecting the two blocks must sit perfectly at the vertical middle of the box: `y1 = y2 = Step_Y + 40`.
  * If Left-to-Right transaction: `x1 = Center_X(Sender) + 75` and `x2 = Center_X(Receiver) - 75`.
  * If Right-to-Left transaction: `x1 = Center_X(Sender) - 75` and `x2 = Center_X(Receiver) + 75`.
  * Append `marker-end="url(#arrow-[TYPE])"`.

- Multi-Segment Loop-Back Curves:
  If a `RESTART_FAIL` step triggers a recursive loopback, draw a dashed crimson path (`#e53935`) exiting the right edge of the source box (`x = Center_X + 75`), routing into the outer right canvas margin channel (X = 960 to 1020), traveling vertically up, and entering the side wall of the target destination block.